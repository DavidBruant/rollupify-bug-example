# rollupify-bug-example

```sh

git clone git@github.com:DavidBruant/rollupify-bug-example.git
cd rollupify-bug-example
npm i
npm run watch-babel
# works, cool, ^C

npm run watch-roll
# Weird warning :
# 'lodash.memoize' is imported by main.js.tmp, but could not be resolved – treating it as an external dependency

```
